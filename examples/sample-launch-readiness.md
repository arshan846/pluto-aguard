# Agent Launch Readiness Packet
Generated by Pluto AgentGuard v0.7.0 | 2026-05-19

## Agent Overview
- **Project path:** ./examples
- **Config file:** ./examples\insecure-agent-config.yaml
- **Policy file:** ./examples\agent-policy.yaml

## Risk Summary
- **Overall Risk Score:** 100.0/100 (Critical)
- **Findings:** 0 critical, 17 high, 1 medium, 0 low

## Security Findings
### HIGH: Agent 'data-analyst-agent' has tools but no declared permissions
- **OWASP ID:** MCP02:2025
- **Description:** Agent 'data-analyst-agent' has 6 tools configured but no explicit permission declarations. Without declared permissions, drift detection is impossible and the agent may operate with implicit full access.
- **Remediation:** Add an explicit 'permissions' section declaring the minimum required access level for each tool. Use the principle of least privilege.

### HIGH: Dangerous tools without human approval on 'data-analyst-agent'
- **OWASP ID:** MCP05:2025
- **Description:** Agent 'data-analyst-agent' can invoke dangerous tools (file_write, execute) without requiring human approval. These tools can cause irreversible changes and should have a human-in-the-loop gate.
- **Remediation:** Add these tools to the 'require_human_approval' list: file_write, execute

### HIGH: Hardcoded Anthropic Key detected
- **OWASP ID:** MCP01:2025
- **Description:** A Anthropic Key pattern was found in .env at line 2. Hardcoded secrets in agent configurations can be extracted through prompt injection or log exposure.
- **Remediation:** Use environment variables or a secrets manager (e.g., Azure Key Vault, AWS Secrets Manager) instead of hardcoding credentials. Rotate this credential immediately.

### HIGH: Hardcoded Connection String detected
- **OWASP ID:** MCP01:2025
- **Description:** A Connection String pattern was found in .env at line 4. Hardcoded secrets in agent configurations can be extracted through prompt injection or log exposure.
- **Remediation:** Use environment variables or a secrets manager (e.g., Azure Key Vault, AWS Secrets Manager) instead of hardcoding credentials. Rotate this credential immediately.

### HIGH: Hardcoded Database Password detected
- **OWASP ID:** MCP01:2025
- **Description:** A Database Password pattern was found in agent.py at line 12. Hardcoded secrets in agent configurations can be extracted through prompt injection or log exposure.
- **Remediation:** Use environment variables or a secrets manager (e.g., Azure Key Vault, AWS Secrets Manager) instead of hardcoding credentials. Rotate this credential immediately.

### HIGH: Hardcoded Generic API Key detected
- **OWASP ID:** MCP01:2025
- **Description:** A Generic API Key pattern was found in .env at line 1. Hardcoded secrets in agent configurations can be extracted through prompt injection or log exposure.
- **Remediation:** Use environment variables or a secrets manager (e.g., Azure Key Vault, AWS Secrets Manager) instead of hardcoding credentials. Rotate this credential immediately.

### HIGH: Hardcoded Generic API Key detected
- **OWASP ID:** MCP01:2025
- **Description:** A Generic API Key pattern was found in .env at line 2. Hardcoded secrets in agent configurations can be extracted through prompt injection or log exposure.
- **Remediation:** Use environment variables or a secrets manager (e.g., Azure Key Vault, AWS Secrets Manager) instead of hardcoding credentials. Rotate this credential immediately.

### HIGH: Hardcoded Generic API Key detected
- **OWASP ID:** MCP01:2025
- **Description:** A Generic API Key pattern was found in insecure-agent-config.yaml at line 32. Hardcoded secrets in agent configurations can be extracted through prompt injection or log exposure.
- **Remediation:** Use environment variables or a secrets manager (e.g., Azure Key Vault, AWS Secrets Manager) instead of hardcoding credentials. Rotate this credential immediately.

### HIGH: Hardcoded Generic API Key detected
- **OWASP ID:** MCP01:2025
- **Description:** A Generic API Key pattern was found in agent.py at line 8. Hardcoded secrets in agent configurations can be extracted through prompt injection or log exposure.
- **Remediation:** Use environment variables or a secrets manager (e.g., Azure Key Vault, AWS Secrets Manager) instead of hardcoding credentials. Rotate this credential immediately.

### HIGH: Hardcoded Generic Secret detected
- **OWASP ID:** MCP01:2025
- **Description:** A Generic Secret pattern was found in .env at line 3. Hardcoded secrets in agent configurations can be extracted through prompt injection or log exposure.
- **Remediation:** Use environment variables or a secrets manager (e.g., Azure Key Vault, AWS Secrets Manager) instead of hardcoding credentials. Rotate this credential immediately.

### HIGH: Hardcoded Generic Secret detected
- **OWASP ID:** MCP01:2025
- **Description:** A Generic Secret pattern was found in .env at line 5. Hardcoded secrets in agent configurations can be extracted through prompt injection or log exposure.
- **Remediation:** Use environment variables or a secrets manager (e.g., Azure Key Vault, AWS Secrets Manager) instead of hardcoding credentials. Rotate this credential immediately.

### HIGH: Hardcoded Generic Secret detected
- **OWASP ID:** MCP01:2025
- **Description:** A Generic Secret pattern was found in insecure-agent-config.yaml at line 24. Hardcoded secrets in agent configurations can be extracted through prompt injection or log exposure.
- **Remediation:** Use environment variables or a secrets manager (e.g., Azure Key Vault, AWS Secrets Manager) instead of hardcoding credentials. Rotate this credential immediately.

### HIGH: Hardcoded Generic Secret detected
- **OWASP ID:** MCP01:2025
- **Description:** A Generic Secret pattern was found in agent.py at line 13. Hardcoded secrets in agent configurations can be extracted through prompt injection or log exposure.
- **Remediation:** Use environment variables or a secrets manager (e.g., Azure Key Vault, AWS Secrets Manager) instead of hardcoding credentials. Rotate this credential immediately.

### HIGH: Hardcoded GitHub Token detected
- **OWASP ID:** MCP01:2025
- **Description:** A GitHub Token pattern was found in insecure-agent-config.yaml at line 32. Hardcoded secrets in agent configurations can be extracted through prompt injection or log exposure.
- **Remediation:** Use environment variables or a secrets manager (e.g., Azure Key Vault, AWS Secrets Manager) instead of hardcoding credentials. Rotate this credential immediately.

### HIGH: Hardcoded Hugging Face Token detected
- **OWASP ID:** MCP01:2025
- **Description:** A Hugging Face Token pattern was found in .env at line 3. Hardcoded secrets in agent configurations can be extracted through prompt injection or log exposure.
- **Remediation:** Use environment variables or a secrets manager (e.g., Azure Key Vault, AWS Secrets Manager) instead of hardcoding credentials. Rotate this credential immediately.

### HIGH: Hardcoded Slack Token detected
- **OWASP ID:** MCP01:2025
- **Description:** A Slack Token pattern was found in insecure-agent-config.yaml at line 24. Hardcoded secrets in agent configurations can be extracted through prompt injection or log exposure.
- **Remediation:** Use environment variables or a secrets manager (e.g., Azure Key Vault, AWS Secrets Manager) instead of hardcoding credentials. Rotate this credential immediately.

### HIGH: Hardcoded Slack Token detected
- **OWASP ID:** MCP01:2025
- **Description:** A Slack Token pattern was found in agent.py at line 13. Hardcoded secrets in agent configurations can be extracted through prompt injection or log exposure.
- **Remediation:** Use environment variables or a secrets manager (e.g., Azure Key Vault, AWS Secrets Manager) instead of hardcoding credentials. Rotate this credential immediately.

### MEDIUM: No timeout or rate limits on agent 'data-analyst-agent'
- **OWASP ID:** N/A
- **Description:** Agent 'data-analyst-agent' has no timeout or rate limit configured. Without limits, a compromised or misbehaving agent can consume unbounded resources or execute an unlimited number of actions.
- **Remediation:** Add 'timeout' (e.g., 300 seconds) and 'rate_limit' (e.g., 100 calls/minute) to constrain agent behavior.

## Tool Permissions
- `sql_query`: undeclared
- `file_write`: undeclared
- `execute`: undeclared
- `send_message`: undeclared
- `create_issue`: undeclared
- `merge_pr`: undeclared

## Policy Coverage
- **Allowed tools:** sql_query, send_message, create_issue
- **Denied tools:** execute, shell, sudo, file_delete
- **HITL requirements:** file_write, merge_pr, deploy
- **Data access rules:** analytics_db (read), user_data (none), financial_data (none)

## Required Mitigations
- [ ] Agent 'data-analyst-agent' has tools but no declared permissions — Add an explicit 'permissions' section declaring the minimum required access level for each tool. Use the principle of least privilege.
- [ ] Dangerous tools without human approval on 'data-analyst-agent' — Add these tools to the 'require_human_approval' list: file_write, execute
- [ ] Hardcoded Anthropic Key detected — Use environment variables or a secrets manager (e.g., Azure Key Vault, AWS Secrets Manager) instead of hardcoding credentials. Rotate this credential immediately.
- [ ] Hardcoded Connection String detected — Use environment variables or a secrets manager (e.g., Azure Key Vault, AWS Secrets Manager) instead of hardcoding credentials. Rotate this credential immediately.
- [ ] Hardcoded Database Password detected — Use environment variables or a secrets manager (e.g., Azure Key Vault, AWS Secrets Manager) instead of hardcoding credentials. Rotate this credential immediately.
- [ ] Hardcoded Generic API Key detected — Use environment variables or a secrets manager (e.g., Azure Key Vault, AWS Secrets Manager) instead of hardcoding credentials. Rotate this credential immediately.
- [ ] Hardcoded Generic API Key detected — Use environment variables or a secrets manager (e.g., Azure Key Vault, AWS Secrets Manager) instead of hardcoding credentials. Rotate this credential immediately.
- [ ] Hardcoded Generic API Key detected — Use environment variables or a secrets manager (e.g., Azure Key Vault, AWS Secrets Manager) instead of hardcoding credentials. Rotate this credential immediately.
- [ ] Hardcoded Generic API Key detected — Use environment variables or a secrets manager (e.g., Azure Key Vault, AWS Secrets Manager) instead of hardcoding credentials. Rotate this credential immediately.
- [ ] Hardcoded Generic Secret detected — Use environment variables or a secrets manager (e.g., Azure Key Vault, AWS Secrets Manager) instead of hardcoding credentials. Rotate this credential immediately.
- [ ] Hardcoded Generic Secret detected — Use environment variables or a secrets manager (e.g., Azure Key Vault, AWS Secrets Manager) instead of hardcoding credentials. Rotate this credential immediately.
- [ ] Hardcoded Generic Secret detected — Use environment variables or a secrets manager (e.g., Azure Key Vault, AWS Secrets Manager) instead of hardcoding credentials. Rotate this credential immediately.
- [ ] Hardcoded Generic Secret detected — Use environment variables or a secrets manager (e.g., Azure Key Vault, AWS Secrets Manager) instead of hardcoding credentials. Rotate this credential immediately.
- [ ] Hardcoded GitHub Token detected — Use environment variables or a secrets manager (e.g., Azure Key Vault, AWS Secrets Manager) instead of hardcoding credentials. Rotate this credential immediately.
- [ ] Hardcoded Hugging Face Token detected — Use environment variables or a secrets manager (e.g., Azure Key Vault, AWS Secrets Manager) instead of hardcoding credentials. Rotate this credential immediately.
- [ ] Hardcoded Slack Token detected — Use environment variables or a secrets manager (e.g., Azure Key Vault, AWS Secrets Manager) instead of hardcoding credentials. Rotate this credential immediately.
- [ ] Hardcoded Slack Token detected — Use environment variables or a secrets manager (e.g., Azure Key Vault, AWS Secrets Manager) instead of hardcoding credentials. Rotate this credential immediately.

## Launch Approval Checklist
- [ ] All CRITICAL findings resolved
- [ ] All HIGH findings resolved or accepted with justification
- [ ] Human-in-the-loop gates configured for dangerous tools
- [ ] Secrets removed from configuration files
- [ ] Rate limits and timeouts configured
- [ ] Agent policy file created and reviewed
- [ ] Behavioral monitoring enabled
- [ ] Launch approved by: _______________
- [ ] Date: _______________
